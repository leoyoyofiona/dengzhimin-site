---
import SectionLayout from '../../components/SectionLayout.astro';
import { getCollection } from 'astro:content';

const posts = await getCollection('papers');
---

<SectionLayout slug="papers" posts={posts} />
